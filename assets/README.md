# Assets

`BJJ logo.svg` is the Balance Martial Arts — Canary Wharf BJJ badge.

It is kept here as the source of truth. The widget does not load it at
runtime: an optimised copy is inlined directly into `whatsapp-widget.html`
so the snippet stays self-contained. Re-inline it if the logo changes.
